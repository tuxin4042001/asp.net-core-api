import { IdToCategoryPipe } from './id-to-category.pipe';

describe('IdToCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new IdToCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
