import { CategorySumPipe } from './category-sum.pipe';

describe('CategorySumPipe', () => {
  it('create an instance', () => {
    const pipe = new CategorySumPipe();
    expect(pipe).toBeTruthy();
  });
});
