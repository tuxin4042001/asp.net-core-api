import { CategoryToIdPipe } from './category-to-id.pipe';

describe('CategoryToIdPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryToIdPipe();
    expect(pipe).toBeTruthy();
  });
});
